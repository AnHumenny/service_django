SECRET_KEY='secret key'

DATABASE_NAME='DB name'
DATABASE_PORT=DB port
DATABASE_PASSWORD="DB PASSWORD"
DATABASE_USER='admin'

ALLOWED_HOSTS=разрешённые хосты

TINYMCE_URL='url tinymce'

GOOGLE_CLIENT_ID="google client id"
GOOGLE_CLIENT_SECRET="google client secret"
GOOGLE_OAUTH_TOKEN_URL="https://oauth2.googleapis.com/token"
GOOGLE_REDIRECT_URI="http://HOST/google_calendar/callback/"

EMAIL_BACKEND='django.core.mail.backends.smtp.EmailBackend'
ENGINE='django.db.backends.postgresql'
EMAIL_HOST='smtp.MAIL'
EMAIL_HOST_USER='емайл пользователя'
EMAIL_HOST_PASSWORD='host password'
EMAIL_PORT=email port
EMAIL_USE_TLS=True or False
EMAIL_USE_SSL=False or True

CELERY_BROKER_URL='redis://localhost:PORT/0'
CELERY_RESULT_BACKEND='redis://localhost:PORT/0'
CELERY_TIMEZONE='timezone'

ACCESS_TOKEN_LIFETIME=время жизни токена(int, minutes)
REFRESH_TOKEN_LIFETIME=период обновления(int, day)
